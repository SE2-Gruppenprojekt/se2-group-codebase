syntax = "proto3";

package at.aau.serg.android.datastore.proto;

option java_multiple_files = true;

message User {
  string uid = 1;
  string displayName = 2;
  string gameId = 3;
  // string currentLobbyId = 3; possibly added later to make Home -> Waiting Room possible?
}
